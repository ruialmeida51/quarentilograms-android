<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:app="http://schemas.android.com/apk/res-auto"
xmlns:tools="http://schemas.android.com/tools"
android:id="@+id/navigation_graph_authentication"
app:startDestination="@id/fragment_login">

<fragment
    android:id="@+id/fragment_login"
    android:name="com.ruialmeida.quarentilograms.features.login.fragment.LoginFragment"
    android:label="fragment_login"
    tools:layout="@layout/fragment_login">
    <action
        android:id="@+id/action_fragment_login_to_fragment_register"
        app:destination="@id/fragment_register"
        app:enterAnim="@android:anim/fade_in"
        app:exitAnim="@android:anim/fade_out"
        app:popEnterAnim="@android:anim/fade_in"
        app:popExitAnim="@android:anim/fade_out" />
    <action
        android:id="@+id/action_fragment_login_to_activity_authenticated"
        app:destination="@id/activity_authenticated"
        app:enterAnim="@android:anim/fade_in"
        app:exitAnim="@android:anim/fade_out"
        app:popEnterAnim="@android:anim/fade_in"
        app:popExitAnim="@android:anim/fade_out" />
</fragment>

<fragment
    android:id="@+id/fragment_register"
    android:name="com.ruialmeida.quarentilograms.features.register.fragment.RegisterFragment"
    android:label="fragment_register"
    tools:layout="@layout/fragment_register">
    <action
        android:id="@+id/action_fragment_register_to_activity_authenticated"
        app:destination="@id/activity_authenticated"
        app:enterAnim="@android:anim/fade_in"
        app:exitAnim="@android:anim/fade_out"
        app:popEnterAnim="@android:anim/fade_in"
        app:popExitAnim="@android:anim/fade_out" />
</fragment>

<activity
    android:id="@+id/activity_authenticated"
    android:name="com.ruialmeida.quarentilograms.features.authenticated.activity.AuthenticatedActivity"
    android:label="activity_authenticated"
    tools:layout="@layout/activity_authenticated" />

</navigation>